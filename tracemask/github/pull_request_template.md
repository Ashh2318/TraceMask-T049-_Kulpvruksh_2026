## What this changes

## How it was tested
- [ ] `npm test` passes (syntax, extension manifest, server boot, 32-check security probe)
- [ ] Tried the change in the web app and/or the Chrome extension

## Checklist
- [ ] No third-party npm packages added (TraceMask stays zero-dependency)
- [ ] No personal data, credentials or files from `data/` committed
- [ ] `CHANGELOG.md` updated if users will notice the change
